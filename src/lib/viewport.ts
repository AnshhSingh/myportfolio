import type { Viewport } from 'next';

export const defaultViewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  themeColor: "#ffffff",
  colorScheme: "light dark",
  viewportFit: "cover",
};
